# ContentLane Frontend

React and Vite application for the ContentLane marketing-video workflow.

The active flow is:

```text
Website → Brand Profile → Hooks → Scripts → AI Images/Videos → Editor
```

The editor uses Canvas, Web Audio, and MediaRecorder. It does not use Remotion.
